
target="test"
context=""
while [[ $# -gt 0 ]]
do
context="$context $1"
case $1 in
    --reload|--fresh)
        target="domains"
    ;;
    --dev|--npm|--composer)
        target="repositories"
    ;;
esac
shift
done
sudo php "./app/server/$target.php" $context
