#!/bin/bash


case "$command" in
    --install)
        [[ ! $(which electrum) || ! -d "$services_path/electrum" ]] && . $base_path/arc $@ --install electrum
    ;;
    *)
        $services_path/electrum/run_electrum $@
    ;;
esac
