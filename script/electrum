#!/bin/bash
root="$services_path/electrum"
sources="$root/sources"
data="$root/data"
send="$sources/run_electrum"

case "$command" in
    --daemon)
        $send daemon -d -D $data
        $send load_wallet -D $data
    ;;
    *)
        $send $@ --dir=$data
    ;;
esac